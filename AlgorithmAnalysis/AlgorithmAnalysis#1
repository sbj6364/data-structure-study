#include <stdio.h>

int main()
{
	int a, b;

	int m;
	scanf("%d %d", &a, &b);

	while (a >= 0)
	{
		if (a == 0) break;
		a = a - b;
	}
	if (a < 0) m = a + b;
	else m = a;
	
	printf("%d", m);
	return 0;
}
